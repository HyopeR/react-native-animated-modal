import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 300,
    backgroundColor: 'white',
    padding: 20,
    borderRadius: 15,
    gap: 10,
    textAlign: 'center',
  },
  title: {
    fontSize: 18,
    fontWeight: 700,
    textAlign: 'center',
  },
  description: {
    textAlign: 'center',
    fontWeight: 400,
    lineHeight: 20,
  },
  action: {
    gap: 10,
    marginTop: 5,
  },
});
