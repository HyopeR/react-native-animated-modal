import {ScrollView, ScrollViewProps} from 'react-native';

export namespace ScrollableViewNs {
  /**
   * ScrollView reference type.
   */
  export type Ref = ScrollView;
  /**
   * ScrollView props type.
   */
  export type Props = ScrollViewProps;
}
